#include <stdio.h>
/**
 * main - write the character c to stdout
 * Return: On success 1.
 * On error, -1 is returned, and erroe is set appropriately.
 */
int main(void)
{
	printf("_putchar\n");
	return (0);
}
