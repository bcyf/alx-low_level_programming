#include "main.h"
/**
 * _abs - value of an integer
 * @n: function that computes the absolute vale of an integer
 * Return: o
 */

int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
